import { NotoWebView } from '../src/components/NotoWebView';

/**
 * The application. Everything it shows comes from `@noto/ui`, which is the
 * same interface the web and desktop builds render.
 */
export default function IndexScreen() {
  return <NotoWebView />;
}
