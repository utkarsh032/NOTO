{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Noto Base",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "moduleDetection": "force",
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "isolatedModules": true,
    // Relative imports in the shared packages carry their `.ts` extension, so
    // Deno can resolve them when an Edge Function imports the same source the
    // applications do. Safe because these packages never emit: they are
    // compiled by the consuming bundler.
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true
  },
  "exclude": ["node_modules", "dist", "build", "out", "coverage", ".turbo"]
}
